[Unit]
Description=Transmind PI Flask app (example)
After=network.target

[Service]
# Değiştirin: çalıştırmak istediğiniz kullanıcı
User=transmind
# Nginx genelde 'www-data' grubunu kullanır; socket erişimi için uygun grup atayın
Group=www-data
WorkingDirectory=/home/transmind/Desktop/Transmind-PI
# ExecStart: sistemdeki gunicorn yolu veya venv içindeki gunicorn yolunu kullanın
# Örnek (sistem gunicorn):
ExecStart=/usr/bin/gunicorn --workers 1 --bind unix:/home/transmind/Desktop/Transmind-PI/app.sock app:app
# Eğer venv içindeki gunicorn'u kullanacaksanız aşağıdakine benzeyecek şekilde değiştirin:
# ExecStart=/home/transmind/Desktop/Transmind-PI/TM/bin/gunicorn --workers 1 --bind unix:/home/transmind/Desktop/Transmind-PI/app.sock app:app
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target

